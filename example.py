listaa = input("Dígame un numero: ")# Creamos el input donde acepte el numero
def createList(n):
    lista = []
    for i in n:
        lista = listaa[0]+len(lista)
    return(lista)
print(createList(listaa))
